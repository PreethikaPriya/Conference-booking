<div class="page-header">
<h3> Edit Conference Rooms </h3>
</div>

<%= form_for @conference_room do |f| %>
	<% if @conference_room.errors.any? %>
	 	<h4> These errors are prohibited from being saved </h4>
	 	<ul style="color:red;">
	 		<% @conference_room.errors.full_messages.each do |msg| %>
	 	 		<li> <%= msg %> </li>
	 	 	<% end %>
	 	 </ul>
	 <% end %>
<% end %>


<%= form_for @conference_room do |f| %>

<label> Conference Room name </label> <br/>
<%= f.text_field :name %> <br/>

<label> Building_id </label><br/>
<%= f.collection_select :building_id,Building.all,:id,:name %> <br/>

<label> Floor </label> <br/>
<%= f.text_field :floor %> <br/>

<label> Landmark </label> <br/>
<%= f.text_field :landmark %> <br/>

<%= f.submit %>

<% end %>

